package uni.usic.domain.entity.users.enums;

public enum SignUpResult {
    SUCCESS,
    USERNAME_REQUIRED,
    PASSWORD_REQUIRED,
    FULLNAME_REQUIRED,
    EMAIL_REQUIRED,
    USERNAME_ALREADY_EXISTS,
    SERVER_ERROR
}
